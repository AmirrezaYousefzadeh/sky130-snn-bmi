// GENERATED by sw/gen_variant.py from rtl/bmi_snn_top.v — do not edit; variant bmi_snn_topg: {} ['GATE_MEM']
`define GATE_MEM
// bmi_snn_top — event-driven streaming SNN decoder for intracortical BMI (NeuroBench primate reaching).
//
// 96 input channels -> H LIF neurons -> 2 leaky-integrator outputs (finger velocity), integer arithmetic.
// Weights (int8) live in one SRAM22 2048x32 macro:  W1[c][4w+l] @ word c*16+w byte l (c=0..96, row 96 = bias),
//                                                    W2[j][i]    @ word 1600+j byte i.
// Two operating modes on the same netlist:
//   mode_dense=0  event-driven: every accepted input event (channel id) immediately streams the 16 weight
//                 words of its row and accumulates into the H membranes (activity-proportional work).
//   mode_dense=1  dense baseline: events only set bits of an input vector; on tick all 96 rows are visited
//                 (multiply-by-0/1 gating), i.e. what a conventional MAC engine does without zero-skipping.
// tick (end of 4 ms bin): bias row, then a two-stage pipelined leak/threshold/reset scan of the H neurons
// (one per cycle); each hidden spike fetches its W2 word and accumulates the two outputs; out_valid pulses with y0/y1.
//
// Clock gating: an always-on enable register drives an integrated clock gate; everything but that
// register (FSM, neuron state, SRAM) runs on core_clk, which stops whenever the core is idle.
// `GATE_MEM (H = 64): the membrane bank is split into 4 groups of 16 lanes (four weight words), each on its own
// integrated clock gate that lets an edge through only in the cycle in which a lane of the group is written (the
// accumulation of one of its weight words, or the leak/threshold/reset of one of its neurons in the tick scan): about
// 4 of the 16 accumulation cycles of an event instead of all of them. Without it the 64 x 20 membrane flip-flops are
// clocked in every one of the ~185 cycles of a bin although only four change per cycle.
`timescale 1ns/1ps

module bmi_snn_topg #(
  parameter integer H     = 64,     // hidden neurons (multiple of 4)
  parameter integer THETA = 256,    // firing threshold (membrane units)
  parameter integer K1    = 4,      // hidden leak: v -= v >>> K1
  parameter integer K2    = 4       // output leak: o -= o >>> K2
)(
  input  wire        clk,
  input  wire        reset,        // synchronous, active high
  input  wire        mode_dense,
  // input events (AER-style handshake)
  input  wire        ev_valid,
  input  wire [6:0]  ev_ch,
  output wire        ev_ready,
  // end-of-bin tick (handshake)
  input  wire        tick,
  output wire        tick_ready,
  // decoded output (two int24 accumulator values)
  output reg         out_valid,
  output wire [23:0] y0,
  output wire [23:0] y1,
  output wire        busy,
  output wire        core_clk_en,
  // weight-memory write port (host load; idle-only)
  input  wire        wr_en,
  input  wire [10:0] wr_addr,
  input  wire [31:0] wr_data
);
  localparam integer NW      = H / 4;          // words per W1 row
  localparam integer W2_BASE = 1600;
  localparam [1:0] S_IDLE = 2'd0, S_ROW = 2'd1, S_NSCAN = 2'd2, S_OUT = 2'd3;

  // ------------------------------------------------------------------ always-on domain
  reg  [1:0]  state;
  reg         rd_pend;
  reg         acc_valid;
  reg         scan_valid;
  reg         en_q;
  wire        en_d = reset | ev_valid | tick | wr_en | (state != S_IDLE) | rd_pend | acc_valid | out_valid | scan_valid;
  always @(posedge clk) en_q <= en_d;
  assign core_clk_en = en_q;

  wire core_clk;
  sky130_fd_sc_hd__dlclkp_4 u_icg (.CLK(clk), .GATE(en_q), .GCLK(core_clk));

  // ------------------------------------------------------------------ gated domain
  reg  [6:0]        row;        // current W1 row (0..96)
  reg  [3:0]        widx;       // word issue counter
  reg               in_tick;
  reg               row_gate;   // accumulate enable for the row being streamed
  reg  [95:0]       x;          // dense-mode input vector
  reg  [6:0]        j;          // neuron scan index
  reg  [3:0]        rd_widx;
  reg               rd_gate;
  reg               rd_is_w2;
  reg  [31:0]       dout_r;        // registered SRAM output (breaks the macro -> adder -> membrane path)
  reg  [3:0]        acc_widx;
  reg               acc_gate, acc_is_w2;
`ifndef GATE_MEM
  reg  signed [19:0] v [0:H-1];
`endif
  reg  signed [23:0] o0, o1;
  // neuron-scan pipeline (stage A: read membrane; stage B: leak / threshold / reset / W2 fetch)
  reg  signed [19:0] vj_r;
  reg  [6:0]         j_r;
  reg  [1:0]         out_cnt;

  assign y0 = o0;
  assign y1 = o1;
  assign busy = (state != S_IDLE) | rd_pend | acc_valid | scan_valid;
  wire idle_awake = (state == S_IDLE) & en_q & ~wr_en;
  assign ev_ready   = idle_awake;
  assign tick_ready = idle_awake & ~ev_valid;

  // SRAM control (registered-output macro: dout valid the cycle after ce)
  reg          mem_ce, mem_we;
  reg  [10:0]  mem_addr;
  wire [31:0]  mem_dout;
  wire         wmem_ce = mem_ce;
  wire         wmem_we = mem_we;

  // saturating 20-bit add of an int8
  function signed [19:0] sat_add20(input signed [19:0] a, input signed [7:0] w);
    reg signed [20:0] s;
    begin
      s = {a[19], a} + {{13{w[7]}}, w};
      if (s > 21'sd524287)       sat_add20 = 20'sd524287;
      else if (s < -21'sd524288) sat_add20 = -20'sd524288;
      else                       sat_add20 = s[19:0];
    end
  endfunction
  // saturating 24-bit add of an int8
  function signed [23:0] sat_add24(input signed [23:0] a, input signed [7:0] w);
    reg signed [24:0] s;
    begin
      s = {a[23], a} + {{17{w[7]}}, w};
      if (s > 25'sd8388607)       sat_add24 = 24'sd8388607;
      else if (s < -25'sd8388608) sat_add24 = -24'sd8388608;
      else                        sat_add24 = s[23:0];
    end
  endfunction

  // neuron-scan datapath (stage B operates on the registered membrane vj_r)
  wire signed [19:0] vl  = vj_r - (vj_r >>> K1);
  wire               spk = (vl >= THETA);

`ifdef GATE_MEM
  // ---- membrane bank in NG = 4 groups of 16 lanes (four weight words), one integrated clock gate per group (see header).
  // Four groups of 320 flip-flops rather than sixteen of 80: TritonCTS builds no buffer tree below a gate with few, widely
  // spread sinks, and the resulting 2 ns slew on the gated net broke hold and setup; gated trees of 512-1024 sinks are
  // the configuration proven in the parallel cores.
  localparam integer NG = 4, LPG = H / NG;                 // groups, lanes per group (16)
  reg  [20*H-1:0]    vflat;
  wire [NG-1:0]      vg_en, vg_clk;
  wire signed [19:0] v_rd = vflat[20*j[5:0] +: 20];        // tick-scan stage A read
  // the four saturating adders are shared by all groups (as in the ungated core): they add the weight word to the
  // membranes of the word selected by acc_widx, and the lanes of that word capture the result when their group's gate opens.
  // The 16-to-1 read of that word is registered one cycle earlier (with rd_widx, when the SRAM output is registered) so that
  // the accumulate cycle contains only the adders and the lane multiplexers - with the gated groups the read multiplexer,
  // the adders and the extra lane select no longer fit in one slow-corner cycle. No hazard: a word is written at most once
  // per 16 cycles and never in the cycle in which it is read for the next word of the same lane.
  reg  [79:0]        v_acc_r;
  always @(posedge core_clk) if (rd_pend) v_acc_r <= vflat[80*rd_widx +: 80];
  wire signed [19:0] v_new [0:3];
  genvar ga;
  generate for (ga = 0; ga < 4; ga = ga + 1) begin : g_acc
    assign v_new[ga] = sat_add20(v_acc_r[20*ga +: 20], dout_r[8*ga +: 8]);
  end endgenerate
  // tick-scan write stage C: the leak/threshold result of stage B is registered and written one cycle later, so that the
  // subtract, the compare and the fan-out to 64 lanes of write multiplexers do not share one cycle (22 ns at the slow
  // corner otherwise). Nothing reads a neuron between its stage-B and stage-C cycles, and the W2 read is still issued in
  // stage B, so the state machine and the output timing are unchanged.
  reg                sc_valid;
  reg  [6:0]         sc_j;
  reg  signed [19:0] sc_v;
  always @(posedge core_clk) begin
    sc_valid <= scan_valid & ~reset; sc_j <= j_r; sc_v <= spk ? 20'sd0 : vl;
  end
  genvar gw;
  generate for (gw = 0; gw < NG; gw = gw + 1) begin : g_vg
    assign vg_en[gw] = reset | (acc_valid & ~acc_is_w2 & acc_gate & (acc_widx[3:2] == gw)) | (sc_valid & (sc_j[5:4] == gw));
    always @(posedge vg_clk[gw]) begin : blk
      integer l;
      for (l = 0; l < LPG; l = l + 1) begin              // constant slices only (see sw/gen_variant.py note on Yosys drivers)
        if (reset) vflat[20*(LPG*gw + l) +: 20] <= 20'd0;
        else if (sc_valid && sc_j[5:4] == gw) begin
          if (sc_j[3:0] == l) vflat[20*(LPG*gw + l) +: 20] <= sc_v;                        // the other 15 lanes hold
        end else if (acc_widx[1:0] == l / 4) vflat[20*(LPG*gw + l) +: 20] <= v_new[l % 4]; // the 4 lanes of the word
      end
    end
  end endgenerate
  // clock gates instantiated flat (no generate scope) so that the hardened netlist has no dotted instance names; H = 64
  sky130_fd_sc_hd__dlclkp_4 u_icg_v0 (.CLK(core_clk), .GATE(vg_en[0]), .GCLK(vg_clk[0]));
  sky130_fd_sc_hd__dlclkp_4 u_icg_v1 (.CLK(core_clk), .GATE(vg_en[1]), .GCLK(vg_clk[1]));
  sky130_fd_sc_hd__dlclkp_4 u_icg_v2 (.CLK(core_clk), .GATE(vg_en[2]), .GCLK(vg_clk[2]));
  sky130_fd_sc_hd__dlclkp_4 u_icg_v3 (.CLK(core_clk), .GATE(vg_en[3]), .GCLK(vg_clk[3]));
`endif

  // control: which read to issue this cycle
  reg        issue_row;        // issue W1 word (row, widx)
  reg        last_word;
  integer    l, n;

  always @(*) begin
    issue_row = (state == S_ROW);
    last_word = (widx == NW - 1);
    mem_ce   = 1'b0;
    mem_we   = 1'b0;
    mem_addr = 11'd0;
    if (state == S_IDLE && wr_en) begin
      mem_ce = 1'b1; mem_we = 1'b1; mem_addr = wr_addr;
    end else if (issue_row) begin
      mem_ce = 1'b1; mem_addr = {row, 4'd0} + {7'd0, widx};
    end else if (scan_valid && spk) begin
      mem_ce = 1'b1; mem_addr = W2_BASE + {4'd0, j_r};
    end
  end

  always @(posedge core_clk) begin
    if (reset) begin
      state <= S_IDLE; row <= 7'd0; widx <= 4'd0; in_tick <= 1'b0; row_gate <= 1'b0;
      x <= 96'd0; j <= 7'd0; rd_pend <= 1'b0; rd_widx <= 4'd0; rd_gate <= 1'b0; rd_is_w2 <= 1'b0;
      out_valid <= 1'b0; o0 <= 24'sd0; o1 <= 24'sd0;
      vj_r <= 20'sd0; j_r <= 7'd0; scan_valid <= 1'b0; out_cnt <= 2'd0;
      dout_r <= 32'd0; acc_widx <= 4'd0; acc_gate <= 1'b0; acc_is_w2 <= 1'b0; acc_valid <= 1'b0;
`ifndef GATE_MEM
      for (n = 0; n < H; n = n + 1) v[n] <= 20'sd0;
`endif
    end else begin
      out_valid <= 1'b0;
      // ---------------- read-data pipeline: issue -> (SRAM latency) -> register dout -> accumulate
      rd_pend <= 1'b0;
      acc_valid <= rd_pend;
      if (rd_pend) begin
        dout_r <= mem_dout; acc_widx <= rd_widx; acc_gate <= rd_gate; acc_is_w2 <= rd_is_w2;
      end
      if (acc_valid) begin
        if (!acc_is_w2) begin
`ifndef GATE_MEM
          if (acc_gate)
            for (l = 0; l < 4; l = l + 1)
              v[{acc_widx, 2'd0} + l] <= sat_add20(v[{acc_widx, 2'd0} + l], dout_r[8*l +: 8]);
`endif
        end else begin
          o0 <= sat_add24(o0, dout_r[7:0]);
          o1 <= sat_add24(o1, dout_r[15:8]);
        end
      end
      // ---------------- neuron-scan stage B (independent of state)
      scan_valid <= 1'b0;
      if (scan_valid) begin
`ifndef GATE_MEM
        v[j_r[5:0]] <= spk ? 20'sd0 : vl;
`endif
        if (spk) begin rd_pend <= 1'b1; rd_is_w2 <= 1'b1; end
      end
      // ---------------- FSM
      case (state)
        S_IDLE: begin
          if (wr_en) begin
            // memory write handled combinationally
          end else if (ev_valid) begin
            if (mode_dense) begin
              x[ev_ch] <= 1'b1;
            end else begin
              row <= ev_ch; row_gate <= 1'b1; widx <= 4'd0; state <= S_ROW; in_tick <= 1'b0;
            end
          end else if (tick) begin
            in_tick <= 1'b1; widx <= 4'd0; state <= S_ROW;
            if (mode_dense) begin row <= 7'd0;  row_gate <= x[0]; end
            else            begin row <= 7'd96; row_gate <= 1'b1; end
          end
        end
        S_ROW: begin
          rd_pend <= 1'b1; rd_widx <= widx; rd_gate <= row_gate; rd_is_w2 <= 1'b0;
          widx <= widx + 4'd1;
          if (last_word) begin
            widx <= 4'd0;
            if (!in_tick) begin
              state <= S_IDLE;
            end else if (row == 7'd96) begin
              state <= S_NSCAN; j <= 7'd0;
            end else begin
              row <= row + 7'd1;
              row_gate <= (row == 7'd95) ? 1'b1 : x[row + 7'd1];
            end
          end
        end
        S_NSCAN: begin
          // stage A: read one membrane per cycle
          if (j == 7'd0) begin
            o0 <= o0 - (o0 >>> K2);
            o1 <= o1 - (o1 >>> K2);
          end
`ifdef GATE_MEM
          vj_r <= v_rd; j_r <= j; scan_valid <= 1'b1;
`else
          vj_r <= v[j[5:0]]; j_r <= j; scan_valid <= 1'b1;
`endif
          j <= j + 7'd1;
          if (j == H - 1) begin state <= S_OUT; out_cnt <= 2'd0; end
        end
        S_OUT: begin
          // cycle 1: stage B of the last neuron (may issue its W2 read); cycle 2: register dout; cycle 3: accumulate
          out_cnt <= out_cnt + 2'd1;
          if (out_cnt == 2'd2) begin
            state <= S_IDLE; in_tick <= 1'b0; x <= 96'd0; out_valid <= 1'b1;
          end
        end
      endcase
    end
  end

  sram22_2048x32m8w8 u_wmem (
    .clk   (core_clk),
    .rstb  (~reset),
    .ce    (wmem_ce),
    .we    (wmem_we),
    .wmask (4'hF),
    .addr  (mem_addr),
    .din   (wr_data),
    .dout  (mem_dout)
  );
endmodule
